import { defineConfig } from '../../../../src/index'

export default defineConfig({
  title: 'AI Links Fixture',
  description: 'Mounted public links.',
  base: '/knowledge/',
  onBrokenLinks: 'ignore',
})
