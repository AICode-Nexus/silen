import type { VirtualConfig } from 'virtual:silen/config'

const config: VirtualConfig = {
  title: 'Test Docs',
  description: '',
  lang: 'en-US',
  base: '/',
  themeConfig: {},
}

export default config
