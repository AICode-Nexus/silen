import { defineConfig } from '../../../../src/index'

export default defineConfig({
  title: 'AI Fixture',
  description: 'Machine-readable documentation.',
  base: '/knowledge',
  privateToken: 'never-publish-this-token',
})
